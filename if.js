let age = 1;



if (age === 18) {
    console.log("Es la primera vez que vas a votar");
} else if (age > 18) {
    console.log("Minimo haz votado una vez");
} else {
    console.log("tas cahvo mijo");
}

condicion ? true : false;

var numero = 1;

var resultado = numero === 1 ? 'es un numero' : 'no es un num';
console.log(resultado);